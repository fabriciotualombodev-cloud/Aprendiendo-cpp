# Aprendiendo C++

Ejercicios de C++ mientras aprendo Desarrollo de Software.

## 🎯 Objetivo

Este repositorio documenta mi progreso aprendiendo C++ como primer lenguaje
de programación, resolviendo ejercicios de sintaxis básica, control de flujo
y estructuras fundamentales.

## 📂 Contenido

### entrada-salida-operadores/
Ejercicios con `cout`, `cin` y operadores aritméticos básicos.

| Archivo | Descripción |
|---|---|
| `hola_mundo.cpp` | Primer programa, salida por consola |
| `celsius_a_fahrenheit.cpp` | Conversión de temperatura |
| `promedio_notas.cpp` | Cálculo de promedio |
| `nota_equivalente.cpp` | Conversión de nota numérica |
| `area_triangulo.cpp` | Cálculo de área de un triángulo |

### condicionales/
Ejercicios con `if`, `if-else` y `switch`.

| Archivo | Descripción |
|---|---|
| `if_ejer_1.cpp` | Estructura if simple |
| `if_ejer_2.cpp` | If-else con dos condiciones |
| `if_ejer_3.cpp` | If-else con múltiples condiciones |
| `if_ejer_4.cpp` | Ejercicio adicional de condicionales |
| `calculadora.cpp` | Calculadora con menú (switch) |

### bucles/
Ejercicios con `for` y `while` (en progreso).

## 🛠️ Cómo compilar y ejecutar

```bash
g++ archivo.cpp -o archivo
./archivo
```

## 📌 Notas

Este repositorio se actualiza a medida que avanzo en nuevos temas.